<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="io.mtini.model.EstateModel"/>
        <import type="java.util.List"/>
        <variable name="estate" type="EstateModel" />
        <variable name="estates" type="List&lt;EstateModel&gt;" />
        <variable name="handler" type="io.mtini.android.adaptor.GenericItemListBindingAdapter" />
    </data>


<RelativeLayout
    android:id="@+id/estatesLayoutView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".EstateActivity">

<!-- The hope was to use a simple Listview adaptor but that didnt seem
to work well due to an attribute setter issue. It is also not clear by example, where
the GenericItemListBindingAdapter is supposed to be initialized. In the Activity class?


    <ListView
        android:id="@+id/estateslistView2"
        app:layout="@layout/estate_layout"
        app:entries="@{estates}"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/linearLayout"/>
-->
</RelativeLayout>
</layout>